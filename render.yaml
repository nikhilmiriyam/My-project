services:
  - type: web
    name: rideshare
    env: node
    plan: free
    buildCommand: npm install && npm install --prefix server && npm install --prefix client
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
