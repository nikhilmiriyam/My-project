services:
  - type: web
    name: rideshare
    env: node
    plan: free
    buildCommand: yarn install && yarn --cwd server install && yarn --cwd client install
    startCommand: yarn start
    envVars:
      - key: NODE_ENV
        value: production
